package com.dailyflow.ui.theme

import androidx.compose.ui.graphics.Color

// Fondo global de la app
val BackgroundApp = Color(0xFFF3EEF8)

// Primario - Rosa vibrante
val PrimaryPink = Color(0xFFF06292)
val PrimaryPinkLight = Color(0xFFFCE4EC)
val PrimaryPinkButton = Color(0xFFF48FB1)

// Secundario - Verde menta
val MintGreen = Color(0xFF80CBC4)
val MintGreenLight = Color(0xFFE0F2F1)

// Colores de prioridad
val PriorityHigh = Color(0xFFE53935)
val PriorityMedium = Color(0xFFFFB300)
val PriorityLow = Color(0xFF43A047)

// Colores del selector de bloque (6 círculos)
val BlockColorPink = Color(0xFFF48FB1)
val BlockColorPurple = Color(0xFFCE93D8)
val BlockColorBlue = Color(0xFF90CAF9)
val BlockColorGreen = Color(0xFF80CBC4)
val BlockColorYellow = Color(0xFFFFF176)
val BlockColorPeach = Color(0xFFFFCC80)

// Superficie
val SurfaceCard = Color(0xFFFFFFFF)
val SurfaceForm = Color(0xFFF8F3FC)

// Texto
val TextPrimary = Color(0xFF1A1A2E)
val TextSecondary = Color(0xFF9E9E9E)
val TextPink = Color(0xFFF06292)

// Acento teal (botón flecha onboarding)
val AccentTeal = Color(0xFF4DB6AC)

// Barra de progreso
val ProgressTrack = Color(0xFFE0E0E0)
val ProgressFill = Color(0xFFF06292)

// Borde lateral de cards de tarea (color por categoría)
val BorderPurple = Color(0xFFCE93D8)
val BorderBlue = Color(0xFF90CAF9)
val BorderGreen = Color(0xFF80CBC4)

// Toggle activo
val ToggleActive = Color(0xFFF06292)

// Iconos BottomNav inactivos
val NavIconInactive = Color(0xFF9E9E9E)
